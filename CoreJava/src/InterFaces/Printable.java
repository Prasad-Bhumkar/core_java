package InterFaces;

public interface Printable {
	//can declare default , static and private methods
	//methods are by default public and abstract (if you still want to implement then make it static , default and private)
	//variables are by default public static final
	void print();

}
