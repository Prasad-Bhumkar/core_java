package lambdaExpression;

public interface Printable {
	//this is a functional interface which can have only one abstract method
	//we can declare any number of other methods but abstract method will be only one
	public void print();
}
